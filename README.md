Mobile touch controls work on any phone/tablet browser
	•	PWA install: Safari (iOS/iPadOS) → Share → “Add to Home Screen” | Chrome (Android) → “Install App”
	•	Opens fullscreen in landscape, works offline after first load
